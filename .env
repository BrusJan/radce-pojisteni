# VPS-native deployment (nginx serves frontend, proxies backend)

# Backend runtime
BACKEND_PORT=8080
POSTGRES_DB=insurance_advisor
POSTGRES_USER=insurance_app
POSTGRES_PASSWORD=6CkSUe_4nQey
POSTGRES_PORT=5432

# CORS — nginx is on the same origin so backend allows localhost proxy
APP_CORS_ALLOWED_ORIGINS=*

# Frontend build — API calls go through nginx proxy on same host
FRONTEND_API_BASE_URL=/api
